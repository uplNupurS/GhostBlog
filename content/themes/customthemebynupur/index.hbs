<html>
<head>
      <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{asset "/gallery.css"}}">
</head>
<body>
    
<header>
    <h1>{{ @site.title}}</h1>
    {{!-- <h1>{{ @site.description}}</h1> --}}
</header>
<main>
    <div class="gallery">
        {{#foreach posts}}
        {{!-- {{> "loop"}} --}}
        <div class="img">
            {{!-- <h1>{{title}}</h1> --}}
            <img src={{feature_image}} alt="" style="">
        </div>

        {{/foreach}}
    </div>
</main>
</body>
</html>